import React from "react";
import styles from "./index.module.css";

interface Props {}

export const Footer = (props: Props) => {
  return <footer className={styles.footer}>Copyright Mateusz Myrcik</footer>;
};
